# MediaStation

A TUI application for managing a self-hosted Jellyfin media server.

## Getting Started

```bash
cargo build --release
cargo run --package mediastation-cli
```

## Structure

- `mediastation-cli` - TUI application
- `mediastation-core` - Core library
